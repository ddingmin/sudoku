# 스도쿠 디자인 시스템 — "키치 팝" (A1)

MZ 타겟 리디자인 확정안. 모든 컴포넌트는 `app/globals.css`의 **시맨틱 토큰과 유틸리티만** 사용한다. 하드코딩된 hex 금지.

## 아이덴티티

- 이름: **스도쿠** (워드마크는 라임 형광펜 스와이프 + "매일 한 판" 스티커)
- 모티프: 체커보드 스트립(`.checker`), 회전 스티커, 하드 오프셋 섀도
- 톤: 웜 화이트 바탕 위 하이퍼블루 + 라임. 도발적이고 가벼운 카피("이 기록, 깰 수 있어?", "자랑 각")

## 색 토큰 (역할 기반)

| 토큰 | 라이트 | 역할 |
|---|---|---|
| `--ground` | #FAF8F3 | 페이지 바탕 |
| `--surface` | #FFFFFF | 카드/셀 표면 |
| `--ink` / `--ink-soft` / `--ink-faint` | #141414 계열 | 텍스트 위계 |
| `--primary` | #2B4CFF | 사용자 숫자, CTA, 뱃지, 클리어 화면 플러드 |
| `--primary-strong` | #1E38CC | 눌림/딥 패턴 |
| `--peer` / `--same` | #EEF2FF / #D8E1FF | 보드 하이라이트 틴트 |
| `--pop` | #C8F04D | 선택 셀, 메모 ON, 형광 스와이프, 메인 공유 버튼 |
| `--danger` / `--danger-wash` | #E93A5E / #FFDDE4 | 오답, 신기록 스티커 |
| `--cell-line` / `--box-line` | #ECE9E0 / #141414 | 보드 헤어라인 / 3x3 구분선 |
| `--edge` / `--shadow-ink` | #141414 | 청키 보더 / 하드 섀도 |

### 다크 모드 ("차콜 + 아이보리 라인")

다크에서도 키치 팝의 형태감(청키 보더·하드 섀도)이 유지되도록, **잉크의 역할이 반전**된다:

| 토큰 | 다크 | 의도 |
|---|---|---|
| `--ground` / `--surface` | #131417 / #1F2126 | 쿨 차콜 — 라임·블루가 탁해지지 않는 바탕 |
| `--ink` | #F2EFE6 | 웜 아이보리 텍스트 |
| `--edge` | **#EDEAE0** | 보더가 아이보리로 반전 — 다크의 청키 아웃라인 |
| `--box-line` | #8B887C | 3x3 구분선 (아이보리 딤) |
| `--shadow-ink` | #000000 | 하드 섀도 (표면이 바탕보다 밝아 단차가 드러남) |
| `--primary` | #5D7CFF | 일렉트릭 블루 — 연보라로 희석하지 않고 채도 유지 |
| `--pop` | #C8F04D 유지 | 시그니처 라임 |
| `--swipe-bg` | 라임 30% 워시 | 아이보리 텍스트 가독 확보 |
| `--danger` | #FF5470 | 체리 라이트 |

원칙: 다크는 라이트의 "회색화"가 아니라 **아이보리 아웃라인 + 일렉트릭 포인트**로 같은 브랜드를 다시 그린 것. 플러드(`--flood`)와 공유 이미지·영상은 테마 무관 고정 팔레트.

## 타이포

- `--font-display` = **Bagel Fat One** (`.font-display`): 워드마크, 타이머, 클리어 타임, 숫자패드 숫자, 큰 헤드라인
- `--font-sans` = **Pretendard**: UI 전반. 라벨은 700~800, 본문 500~600
- 보드 숫자: Pretendard 800 (가독성 우선), 주어진 숫자 `--ink`, 사용자 입력 `--primary`, 오답 `--danger`

## 형태 규칙

- 청키 컴포넌트: `.chunky`(2px 보더 + 3px 하드 섀도), `.chunky-sm`, 큰 카드엔 `--shadow-lg/xl`
- 눌림 인터랙션: `.chunky-press` (translate + 섀도 축소). scale 액티브는 스티커/아이콘에만
- 스티커: `.sticker` + `transform: rotate(±6~8deg)` + 역할 색 배경
- 반경: `--r-sm`(칩) `--r-md`(버튼) `--r-lg`(보드/카드) `--r-xl`(모달 카드)

## 모션

- 숫자 입력 `cell-pop`(스프링+살짝 회전), 오답 `cell-shake`+`error-pulse`
- 행/열/박스 완성 `unit-ripple`(블루), 클리어 `clear-wave`(라임) → confetti(브랜드 4색)
- `prefers-reduced-motion` 존중

## 공유 비주얼

- 클리어 화면/공유 랜딩: `--primary` 플러드 배경 + 상하단 체커(deep tone), 흰 카드 + 스티커
- 공유 카드(PNG/OG): 동일 구성 — 상단 로고+날짜, 중앙 라임 스와이프 위 클리어 타임(display체), 스탯 칩 3개, 체커 푸터
- 카피: "클리어!", "오늘 퍼즐 깼다. 자랑 각.", 랜딩 "이 기록, 깰 수 있어?" / CTA "나도 풀어보기"
